#!/bin/sh

# Build images
podman build \
    -t hub-nginx \
    --file=nginx/Dockerfile \
    --env=../../../.env \
    --no-cache

podman build \
    -t hub-app \
    --file=app/Dockerfile \
    --env=../../../.env \
    --no-cache

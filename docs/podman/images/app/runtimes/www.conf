[www]
; Unix user/group
user = www-data
group = www-data

; Listener
listen = 127.0.0.1:9000

; Process Manager
pm = dynamic
pm.max_children = 5
pm.start_servers = 2
pm.min_spare_servers = 1
pm.max_spare_servers = 3
pm.max_requests = 500

; Logging
access.log = /dev/null
